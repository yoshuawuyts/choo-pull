{
  "name": "choo-pull",
  "version": "1.0.0",
  "description": "Wrap handlers to use pull-stream",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps && NODE_ENV=test node test",
    "test:cov": "standard && npm run deps && NODE_ENV=test istanbul cover test.js"
  },
  "repository": "yoshuawuyts/choo-pull",
  "keywords": [
    "pull",
    "pull-stream",
    "choo",
    "bel",
    "yo-yo",
    "plugin",
    "tiny"
  ],
  "license": "MIT",
  "dependencies": {
    "pull-stream": "^3.5.0"
  },
  "devDependencies": {
    "dependency-check": "^2.5.3",
    "istanbul": "^0.4.4",
    "noop2": "^2.0.0",
    "standard": "^8.4.0",
    "tape": "^4.6.2"
  }
}
